type SearchButtonProps = {
}
export default function SearchButton (props: SearchButtonProps) {
    return (
        <button className="bg-blue-500 hover:bg-blue-700 px-1" id="search" type="submit">search</button>
    );
}
